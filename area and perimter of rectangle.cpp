#include <stdio.h>
 int width;
 int height;
 
 int area;
 int perimeter;
 
 int main()
 { 
     height = a;
     width = b;
     
     perimeter = a + b *(height + width);
     printf("Perimeter of th rectangle = %d inches\n", perimeter);
     
     area = height * width;
     printf("Area of the rectangle = %d square inches\n", area);
     
 return (0);
 }
